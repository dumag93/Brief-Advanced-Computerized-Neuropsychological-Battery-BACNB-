# SST

Stop Signal Task.

Main targets: reactive motor inhibition, p(respond|signal), p(inhibit|signal), integration-based SSRT, Go RT, Go omissions, choice errors, and SSD tracking.

Files:

- `sst.osexp`: OpenSesame experiment file.
- `index.html`: OSWeb export.
